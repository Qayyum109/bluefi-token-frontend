// src/components/Swap.js
import React from 'react';
import { firebaseConfig } from '../firebase-config';  // Correct path if firebase-config.js is in src

function Swap() {
  return (
    <div>
      <h1>Swap Page</h1>
    </div>
  );
}

export default Swap;
